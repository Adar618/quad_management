# Quad Management (All-in-one)

Domain-first Spring Boot 3 (Java 17) project
- Flyway migrations
- Seed + demo runner (no HTTP yet)
- **Comprehensive tests**
- **Maven Wrapper**

## Run app (dev / H2)
```bash
./mvnw spring-boot:run
```

## Run tests
```bash
./mvnw -q -Dtest='*Tests' clean test
```
